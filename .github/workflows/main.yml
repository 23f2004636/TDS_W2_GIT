name: Test Action

on:
  push:
    branches:
      - main  # Change 'main' to your default branch if it's different

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 23f2004636@ds.study.iitm.ac.in
        run: echo "Hello, world!"
